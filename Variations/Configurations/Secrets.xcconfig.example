// Secrets.xcconfig.example
// Variations
//
// 复制为同目录 Secrets.xcconfig 后填值（Secrets.xcconfig 已被 .gitignore 排除，不入库）。
// REGISTER_SECRET 须与后端 .env 的 REGISTER_SECRET 一致；
// 轮换时生成新值（openssl rand -hex 32）并发版，服务端同步切换（需新旧双 secret 过渡窗口）。
// 缺失时 App 可构建，但设备注册会因缺少门槛密钥而失败（fail-safe）。

REGISTER_SECRET =
